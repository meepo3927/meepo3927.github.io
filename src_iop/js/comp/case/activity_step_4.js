/**
 * 创建营销活动 - 步骤4
 * @Vue
 * @date  2017.03.01
 */

define(['vue'], function (Vue) {
	var template = `<div class="activity-step-4">
		step - 4
	</div>`;
	var methods = {};
	var computed = {};
	var onReady = function () {
	};
	var data = function () {
		return {
		};
	};
	return Vue.component('activity-step-4', {
		template: template,
		methods: methods,
		computed: computed,
		data: data,
		props: [],
		mounted: onReady
	});
});