import {Vue} from 'common';

let methods = {};
methods.alert = function () {
};
let mounted = function () {};
window.Index = new Vue({
    el: '#main',
    mounted,
    methods,
    data: {
        name: 'index-123'
    }
});
